Place example input files here if you want a shareable demo dataset.

Expected raw-file naming by default:

- hilic-msms-1.txt
- hilic-msms-1.xlsx
- ...

For v0.3 backend tests, keep demo data small and point both `raw_ms2_dir` and
`precursor_dir` to this folder if the text and target Excel files live together.
